import { createMuiTheme } from "@material-ui/core";

const themes = {
	light: createMuiTheme({
		palette: {

		}
	}),
	dark: createMuiTheme({
		palette: {
			type: "dark",

		}
	})
}

export default themes;